# Optimus - Backend (Express + MongoDB)

## Quick start (local)

Requirements:
- Node 20 (use nvm)
- Docker (for local Mongo) or a MongoDB Atlas connection

1. From repo root:
```bash
cd backend
cp .env.example .env
# Edit .env to set secrets and MONGO_URI (or use Docker local below)